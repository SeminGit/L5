({
    Show : function(messageText) {
        var toastEvent = $A.get("e.force:showToast");
            toastEvent.setParams({
                "title": $A.get("$Label.c.EForceEventTitle"),
                "message": messageText});
        toastEvent.fire();
    }
})
